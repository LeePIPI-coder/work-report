# 本周工作总结（2026-W37）

> 汇总日期：2026-09-08 ｜ 覆盖服务器：mucus-yolo / ansys-airway

## 一、本周主要工作

### 1. 医学影像 AI —— 3D OBB 定向分类实验（mucus-yolo）
- **目标/背景：** 在 3D ResNet 二阶段病变分类基础上，引入 3D 定向包围盒（OBB）进行 patch 定向与分类，并同步整理实验代码与结果到 GitHub。
- **完成内容：**
  - 新增 3D OBB 定向 patch 分类全套脚本：`obb_dataset`、patient5fold 构建/运行/汇总、patch 大小对比、FROC 计算、过拟合子集验证。
  - 新增黏液/血管分析脚本：黏液表构建、非黏液表、统一 patch 采样表、血管 veto 分析、置信度分布。
  - 新增 YOLO 2.5D 数据构建脚本：`build_yolo_25d_vmin`、`make_yolo_dataset`、triplanar QC 采样。
- **结果：** 全部改动（115 个文件）提交并推送至 `LeePIPI-coder/Mucus_yolo`（commit `d737ed5`）。

### 2. 代码仓库整理与工具链（mucus-yolo）
- **目标/背景：** 清理历史遗留数据，规范 git 忽略规则。
- **完成内容：** 清理 `Get_3D_Class_Data` 旧预测 CSV 与旧分类器脚本；更新 `.gitignore` 忽略实验产物（`experiments/`、`cv_folds/`、`3D_obb_data/`）。

### 3. 工作日志系统（mucus-yolo / ansys-airway）
- **目标/背景：** 多服务器汇总的每周工作日志与周报。
- **完成内容：** 在两台服务器安装并初始化 `weekly-work-report` skill：多服务器汇总 + 中心 Git 存储（`work-report.git`），配置 server-id `mucus-yolo` 与 `ansys-airway`；记录今日工作并同步到中心仓库。

### 4. 气道 CFD 封盖管线规划（ansys-airway）
- **目标/背景：** 规划 NIfTI→STL 的 SDF+模糊+Taubin 管线与 airway_cfd 封盖管线改进方向。
- **完成内容：** 查看并规划断支网格级补桥、膨胀保护等改进（未落地新代码）。

## 二、实验与关键结果

| 工作项 | 结果/指标 | 说明 |
|---|---:|---|
| 3D OBB 分类实验代码 | 115 文件提交 | 已推送至 GitHub `d737ed5` |

> 本周无明显量化的实验指标（如 AUPRC 等）待产出；3D OBB 分类实验分析与结果汇总为进行中。

## 三、问题及解决

- **问题：** 中央日志仓库 `work-report.git` 推送时 403（凭证为另一账号）
  - 原因/判断：本机默认凭证为 `LeePIPIx`，无该仓库写权限
  - 处理方式：改用 LeePIPI-coder 的 token 配置推送，成功后恢复干净 URL
  - 当前结果：已解决，日志正常同步
- **问题：** `/workspace/Airway_STL` 不是 Git 仓库，无法注册为项目
  - 处理方式：按 SKILL.md 以 manual 记录类型保存
  - 当前结果：已解决

## 四、当前遗留问题

- 3D OBB 分类实验分析与结果汇总尚未完成。
- 中央 git 日志后续多机同步策略需持续维护（多台服务器可能产生并发记录）。

## 五、下周计划

1. 继续 3D OBB 分类实验的分析与结果汇总（评估指标、patch 大小对比结论文档化）。
2. 落地气道 CFD 封盖管线的具体改进（断支网格级补桥、膨胀保护）。
3. 持续使用周报 skill 记录每日工作，维护中心日志仓库同步。
